# Canonical Requests and Reproducible Replay

## 0:00 - 0:18 — Canonical inputs

**[VISUAL]** Show equivalent JSON objects entering one canonicalization step.
**[EQUATION]** key = SHA-256(canonical(request))
**[CLAIM canonical-source]** Canonical serialization removes irrelevant object-key ordering before hashing.

**[VOICEOVER]**

Canonical serialization gives equivalent request objects the same byte order before hashing. That makes the cache key insensitive to irrelevant object-key ordering.

**Delivery:** Clear, measured, and direct.

## 0:18 - 0:36 — Recorded replay

**[VISUAL]** Show generated bytes entering a content-addressed store and returning during frozen replay.
**[CLAIM replay-source]** Exact replay comes from retaining and verifying accepted output bytes.

**[VOICEOVER]**

Fresh model output may vary. Exact replay instead uses the accepted bytes, verifies their checksum, and prohibits provider calls when the run is frozen.

**Delivery:** Emphasize the distinction between generation and replay.
